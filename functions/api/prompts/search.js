export const SEARCH_PROMPT = `SEARCH MODE.

Use search tools for: latest news, live prices, weather, recent software releases, current events, government updates.
Never search for timeless knowledge, historical facts, or standard syntax.
Deliver synthesized result naturally — never narrate the act of searching.
If a tool fails, state the limitation clearly and offer best available alternative.`;
